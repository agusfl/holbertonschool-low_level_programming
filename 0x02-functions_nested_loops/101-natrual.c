#include <stdio.h>
/*
 * main - print sum of multiples of 3 or 5
 *
 * Result: Zero
 */

int main (void)
{
	int i;
	int suma = 0;

	for (i = 0; i < 1024; i++)
	{
		if ((n % 3 == 0) || (n % 5 == 0))
		{
			suma = i + i;
		}
	}
	printf("%d\n", sum);
	return (0);
}
